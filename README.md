# mertturna.com

Single-page personal website. Plain HTML + CSS, no build step.

- `index.html` — all content lives here; edit the paragraphs and links directly.
- `style.css` — typography and layout.

Deploy anywhere that serves static files (GitHub Pages, Vercel, Netlify, Squarespace code injection, etc.).
